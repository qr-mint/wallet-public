// import { coins } from '@/config';

// export function getCoinByNetwork (network: ICoinNetwork) {
// 	return coins.filter((coin) => coin.network.toLowerCase() === network.toLowerCase())[0];
// }

// export function getCoinByCurrency (currency: ICoinCurrency) {
// 	return coins.filter((coin) => coin.currency.toLowerCase() === currency.toLowerCase())[0];
// }
